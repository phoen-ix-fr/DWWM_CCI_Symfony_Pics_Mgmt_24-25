{# Définition du gabarit TWIG associé à mon composant #}

{#  
    La variable strType est utilisée pour afficher le "type" (couleur) du bouton,
    Cela permet de pouvoir y rajouter l'indication "outline" sur les classes 
#}
<a class="btn btn-{{ strType }}" href="{{ path }}">{{ text }}</a>